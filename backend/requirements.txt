# CPU-only PyTorch build — much smaller download than the default CUDA build.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pillow==10.4.0
torch==2.4.1
torchvision==0.19.1

# Testing
pytest==8.3.2
httpx==0.27.2
